# Built-in LUT Placeholder

v0.1.1 keeps built-in LUTs as descriptors in code so the simulator can start without bundled media.
Real `.cube` files can be added here later and wired into `LUTRepository`.
